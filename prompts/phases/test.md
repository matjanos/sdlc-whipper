Verify the acceptance check on the live preview environment.

## Ticket
{{ticket}}

## Acceptance check
{{acceptance}}

## Preview environment
{{previewUrl}}

Use Playwright for UI flows or plain HTTP for APIs. Evidence must be specific.

End your reply with exactly this JSON block (```json fenced), nothing after it:

```json
{
  "pass": true,
  "evidence": "what you did, what you saw, including any errors encountered"
}
```
