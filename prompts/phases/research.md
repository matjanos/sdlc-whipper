Plan the execution of this ticket.

Worktree (already checked out on a dedicated branch): {{worktree}}

{{ticket}}

STUB NOTE: this is a skeleton prompt — a short but honest plan is expected; real depth arrives in M4.

End your reply with exactly this JSON block (```json fenced), nothing after it:

```json
{
  "confidence": "high or low",
  "questions": ["sharp, specific questions — empty if confidence is high"],
  "planMarkdown": "# Implementation plan\n(steps, files, testing approach, risks)"
}
```
